export default class ValidatorPagesModule {

    handleContentMessage(message, sender, connections) {
        switch (message.name) {
            case 'pageInfo':
                break;
        }
    }
}