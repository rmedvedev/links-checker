# OMTA extension for Chrome Browser #
This extension allows you to do Smoke tests on any site without writing autotests.

## Build project ##
For build project:
```bash
npm install && npm run build
```

